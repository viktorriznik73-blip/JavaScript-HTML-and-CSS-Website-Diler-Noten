if (btnCopy) {
        btnCopy.addEventListener("click", () => {
            const infoText = "Gelingsnachweis Kurs Modulprüfung Klassenarbeit Note 1 Note 2 Note 3 Note 4";
            navigator.clipboard.writeText(infoText)
                .then(() => {
                    btnCopy.innerText = "Copied :)";
                    btnCopy.style.backgroundColor = "white";
                    setTimeout(() => {
                        btnCopy.innerText = "Copy Info";
                        btnCopy.style.backgroundColor = "green";
                    }, 2000);
                })
                .catch(err => {
                    console.log("Mistake :(", err);
                });
        });
    }
if (buttonCopy) {
    buttonCopy.addEventListener("click", () => {
        const textinfo = "Jahren Blutgruppe Jahrgang Sozialverhalten Info1 Info2 Info3 Info4"
        navigator.clipboard.writeText(textinfo)
        .then(() => {
            buttonCopy.innerText = "Copied :)";
            buttonCopy.style.backgroundColor = "white";
            setTimeout(() => {
                buttonCopy.innerText = "Copy Info";
                buttonCopy.style.backgroundColor = "green";
            }, 2000);
        })
        .catch(err => {
               console.log("Mistake :(", err);
        });
    })
}